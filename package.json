{
  "name": "geonorge-wms-viewer",
  "version": "1.0.0",
  "description": "Accessibility data viewer for Geonorge Tilgjengelighet WMS",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "check": "npm run test && npm run build",
    "mobile:init": "npx create-expo-app@latest mobile --template default@sdk-57"
  },
  "dependencies": {
    "@tilgjengelighet/shared": "file:packages/shared",
    "ol": "^10.8.0"
  },
  "devDependencies": {
    "vite": "^8.1.2",
    "vitest": "^4.1.9"
  }
}
